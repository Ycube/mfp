import { SUBMIT } from '../actions';

const initialState = {
  food: '',
  calories: 0,
  meal: ''
}

export default function(state = initialState, aciton){
  switch(aciton.type) {
    case SUBMIT: 
    
  }
}